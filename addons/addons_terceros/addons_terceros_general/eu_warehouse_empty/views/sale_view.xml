<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Inherit Form View to Modify it -->
        <!-- <record id="view_sale_order_wherehouse" model="ir.ui.view">
            <field name="name">view_sale_order_wherehouse</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                
                <xpath expr="//label[@for='commitment_date']" position="replace">
                    <field name="warehouse_id" options="{'no_create': True}" groups="stock.group_stock_multi_warehouses" force_save="1"/>
                    
                </xpath>
            </field>
        </record> -->
    </data>
</odoo>